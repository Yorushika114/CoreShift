# LLM NLU 接入实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 DeepSeek LLM 替换正则 NLU，支持口语化中文/英文语音指令，并新增自然语言日程查询摘要功能。

**Architecture:** 新增两条 API 路由：`/api/llm/parse`（NLU 结构化解析）和 `/api/llm/summarize`（查询摘要生成）；`parseVoiceCommand.ts` 新增异步函数 `parseVoiceCommandWithLLM`，优先调 LLM，失败则静默降级到现有正则；`VoiceCommandOverlay` 加语言切换按钮，query 结果展示 AI 摘要文字区域。

**Tech Stack:** DeepSeek Chat API（OpenAI 兼容格式）、Next.js App Router API Routes、TypeScript

---

## 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `.env.local` | 修改 | 新增 `DEEPSEEK_API_KEY` 配置项 |
| `types/index.ts` | 修改 | `ParsedCommand` 新增 `queryRangeStart?`、`queryRangeEnd?` |
| `app/api/llm/parse/route.ts` | 新建 | LLM NLU 解析端点 |
| `app/api/llm/summarize/route.ts` | 新建 | LLM 查询摘要端点 |
| `lib/voice/parseVoiceCommand.ts` | 修改 | 新增 `parseVoiceCommandWithLLM` 异步函数 |
| `components/voice/VoiceCommandOverlay.tsx` | 修改 | 语言切换、LLM 调用、AI 摘要 UI |
| `__tests__/lib/voice/parseVoiceCommandLLM.test.ts` | 新建 | `parseVoiceCommandWithLLM` 单测 |

---

## Task 1: 环境变量

**Files:**
- Modify: `.env.local`

- [ ] **Step 1: 在 `.env.local` 末尾追加 DeepSeek key 配置**

在 `.env.local` 文件末尾添加：
```
# DeepSeek LLM（NLU 解析 + 查询摘要）
DEEPSEEK_API_KEY=你的key填这里
```

- [ ] **Step 2: 验证 key 可访问**

```bash
curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer $DEEPSEEK_API_KEY" \
  https://api.deepseek.com/models
```
预期输出：`200`（或 `401` 表示 key 填错）

- [ ] **Step 3: Commit**

```bash
git add .env.local
git commit -m "chore: 添加 DeepSeek API key 配置"
```

---

## Task 2: 更新 ParsedCommand 类型

**Files:**
- Modify: `types/index.ts`

- [ ] **Step 1: 在 `ParsedCommand` 中新增查询范围字段**

将 `types/index.ts` 中的 `ParsedCommand` 改为：

```ts
export type ParsedCommand = {
  intent: Intent;
  title?: string;
  startAt?: string;
  endAt?: string;
  reminderAt?: string;
  hasDate?: boolean;
  hasTime?: boolean;
  /** query intent 时 LLM 解析出的查询时间范围起点（ISO 8601） */
  queryRangeStart?: string;
  /** query intent 时 LLM 解析出的查询时间范围终点（ISO 8601） */
  queryRangeEnd?: string;
  ambiguities: string[];
  clarificationNeeded: boolean;
  clarificationQuestion?: string;
};
```

- [ ] **Step 2: 类型检查通过**

```bash
npx tsc --noEmit
```
预期：无报错（新增可选字段，现有代码无需修改）

- [ ] **Step 3: Commit**

```bash
git add types/index.ts
git commit -m "feat: ParsedCommand 新增 queryRangeStart/queryRangeEnd 字段"
```

---

## Task 3: LLM Parse API 路由

**Files:**
- Create: `app/api/llm/parse/route.ts`

- [ ] **Step 1: 创建目录并新建路由文件**

```bash
mkdir -p app/api/llm/parse
```

创建 `app/api/llm/parse/route.ts`：

```ts
import { NextRequest } from 'next/server';

const SYSTEM_PROMPT = `You are a calendar voice command parser. Parse the user's input into a structured JSON object.

Current date and time: {NOW}

Return ONLY valid JSON matching this exact structure (no markdown, no explanation):
{
  "intent": "create" | "delete" | "query" | "modify",
  "title": string | undefined,
  "startAt": string | undefined,
  "endAt": string | undefined,
  "reminderAt": string | undefined,
  "queryRangeStart": string | undefined,
  "queryRangeEnd": string | undefined,
  "hasDate": boolean,
  "hasTime": boolean,
  "ambiguities": string[],
  "clarificationNeeded": boolean,
  "clarificationQuestion": string | undefined
}

Rules:
- All datetime fields must be absolute ISO 8601 strings (e.g. "2026-05-30T15:00:00.000Z")
- For "query" intent: set queryRangeStart and queryRangeEnd to cover the range the user asks about. If no range specified, use today (00:00 to 23:59)
- ambiguities: list any assumptions made (e.g. "No date mentioned, defaulted to today"). Use the same language as the user's input
- clarificationQuestion: only set if title is completely unidentifiable. Use the same language as the user's input
- Output language for all text fields must match the user's input language`;

export async function POST(req: NextRequest) {
  const body = await req.json() as { text?: unknown; lang?: unknown; now?: unknown };
  const { text, lang, now } = body;

  if (!text || typeof text !== 'string') {
    return new Response('Missing text', { status: 400 });
  }

  const apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) {
    return new Response('LLM not configured', { status: 503 });
  }

  const systemPrompt = SYSTEM_PROMPT.replace(
    '{NOW}',
    typeof now === 'string' ? now : new Date().toISOString()
  );

  const langHint = lang === 'en-US'
    ? '\n\nThe user is speaking English. All text fields in the response must be in English.'
    : '\n\nThe user is speaking Chinese. All text fields in the response must be in Chinese.';

  try {
    const upstream = await fetch('https://api.deepseek.com/chat/completions', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'deepseek-chat',
        messages: [
          { role: 'system', content: systemPrompt + langHint },
          { role: 'user', content: text },
        ],
        response_format: { type: 'json_object' },
        temperature: 0.1,
        max_tokens: 512,
      }),
    });

    if (!upstream.ok) {
      console.error('[LLM parse] upstream error', upstream.status);
      return new Response('LLM upstream error', { status: 502 });
    }

    const data = await upstream.json() as {
      choices?: { message?: { content?: string } }[];
    };
    const content = data.choices?.[0]?.message?.content;
    if (!content) return new Response('Empty LLM response', { status: 502 });

    const parsed = JSON.parse(content) as Record<string, unknown>;
    if (!Array.isArray(parsed.ambiguities)) parsed.ambiguities = [];

    return Response.json(parsed);
  } catch (err) {
    console.error('[LLM parse] error', err);
    return new Response('LLM request failed', { status: 502 });
  }
}
```

- [ ] **Step 2: 手动测试端点**

确保 dev server 在跑（`pnpm dev`），然后：
```bash
curl -s -X POST http://localhost:3001/api/llm/parse \
  -H "Content-Type: application/json" \
  -d '{"text":"明天下午三点开组会","lang":"zh-CN","now":"2026-05-30T10:00:00.000Z"}' | jq .
```
预期：返回包含 `intent: "create"`, `title: "组会"`, `startAt` 为 2026-05-31T07:00:00Z 附近的 JSON

- [ ] **Step 3: Commit**

```bash
git add app/api/llm/parse/route.ts
git commit -m "feat: 新增 LLM NLU 解析 API 路由 /api/llm/parse"
```

---

## Task 4: parseVoiceCommandWithLLM 函数 + 单测

**Files:**
- Modify: `lib/voice/parseVoiceCommand.ts`
- Create: `__tests__/lib/voice/parseVoiceCommandLLM.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `__tests__/lib/voice/parseVoiceCommandLLM.test.ts`：

```ts
import { parseVoiceCommandWithLLM } from '@/lib/voice/parseVoiceCommand';

const mockFetch = jest.fn();
global.fetch = mockFetch;

beforeEach(() => mockFetch.mockReset());

describe('parseVoiceCommandWithLLM', () => {
  it('returns LLM result when API succeeds', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        intent: 'create',
        title: '组会',
        startAt: '2026-05-31T07:00:00.000Z',
        hasDate: true,
        hasTime: true,
        ambiguities: [],
        clarificationNeeded: false,
      }),
    });

    const result = await parseVoiceCommandWithLLM('明天下午三点开组会', 'zh-CN', new Date('2026-05-30T10:00:00Z'));

    expect(result.intent).toBe('create');
    expect(result.title).toBe('组会');
    expect(mockFetch).toHaveBeenCalledWith(
      '/api/llm/parse',
      expect.objectContaining({ method: 'POST' })
    );
  });

  it('falls back to regex when API returns 503', async () => {
    mockFetch.mockResolvedValueOnce({ ok: false, status: 503 });

    const result = await parseVoiceCommandWithLLM('明天下午三点开组会', 'zh-CN', new Date('2026-05-30T10:00:00Z'));

    expect(result.intent).toBe('create');
    expect(result.title).toBeDefined();
  });

  it('falls back to regex when fetch throws', async () => {
    mockFetch.mockRejectedValueOnce(new Error('network error'));

    const result = await parseVoiceCommandWithLLM('删除明天的会议', 'zh-CN', new Date('2026-05-30T10:00:00Z'));

    expect(result.intent).toBe('delete');
  });

  it('passes lang to the API', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        intent: 'create',
        title: 'team meeting',
        startAt: '2026-05-31T07:00:00.000Z',
        hasDate: true,
        hasTime: true,
        ambiguities: [],
        clarificationNeeded: false,
      }),
    });

    await parseVoiceCommandWithLLM('meeting tomorrow at 3pm', 'en-US', new Date('2026-05-30T10:00:00Z'));

    const body = JSON.parse((mockFetch.mock.calls[0][1] as RequestInit).body as string);
    expect(body.lang).toBe('en-US');
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
pnpm test __tests__/lib/voice/parseVoiceCommandLLM.test.ts
```
预期：FAIL（`parseVoiceCommandWithLLM` 不存在）

- [ ] **Step 3: 在 `parseVoiceCommand.ts` 末尾添加函数**

在 `lib/voice/parseVoiceCommand.ts` 末尾追加：

```ts
import type { ParsedCommand } from '@/types';

type LLMParsedCommand = ParsedCommand & {
  queryRangeStart?: string;
  queryRangeEnd?: string;
};

/**
 * LLM 优先的语音指令解析。调用 /api/llm/parse，失败时静默降级到正则解析。
 */
export async function parseVoiceCommandWithLLM(
  text: string,
  lang: 'zh-CN' | 'en-US' = 'zh-CN',
  fallbackDate: Date = new Date()
): Promise<LLMParsedCommand> {
  try {
    const res = await fetch('/api/llm/parse', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text, lang, now: fallbackDate.toISOString() }),
    });
    if (!res.ok) throw new Error(`LLM parse failed: ${res.status}`);
    const parsed = await res.json() as LLMParsedCommand;
    if (!Array.isArray(parsed.ambiguities)) parsed.ambiguities = [];
    return parsed;
  } catch {
    return parseVoiceCommand(text, fallbackDate);
  }
}
```

注意：`LLMParsedCommand` 类型和 `import` 已在文件顶部有 `ParsedCommand` 的引用，只需在文件末尾追加函数本体（类型定义写在函数上方）。

- [ ] **Step 4: 运行测试，确认通过**

```bash
pnpm test __tests__/lib/voice/parseVoiceCommandLLM.test.ts
```
预期：4 tests PASS

- [ ] **Step 5: 运行全量测试确认无回归**

```bash
pnpm test
```
预期：全量 PASS

- [ ] **Step 6: Commit**

```bash
git add lib/voice/parseVoiceCommand.ts __tests__/lib/voice/parseVoiceCommandLLM.test.ts
git commit -m "feat: 新增 parseVoiceCommandWithLLM，LLM优先，失败降级正则"
```

---

## Task 5: LLM Summarize API 路由

**Files:**
- Create: `app/api/llm/summarize/route.ts`

- [ ] **Step 1: 创建目录并新建路由文件**

```bash
mkdir -p app/api/llm/summarize
```

创建 `app/api/llm/summarize/route.ts`：

```ts
import { NextRequest } from 'next/server';
import type { CalendarEvent } from '@/types';

export async function POST(req: NextRequest) {
  const body = await req.json() as {
    question?: unknown;
    events?: unknown;
    lang?: unknown;
    rangeStart?: unknown;
    rangeEnd?: unknown;
  };

  const { question, events, lang, rangeStart, rangeEnd } = body;

  if (!question || typeof question !== 'string') {
    return new Response('Missing question', { status: 400 });
  }

  const apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) {
    return new Response('LLM not configured', { status: 503 });
  }

  const eventList = Array.isArray(events) ? (events as CalendarEvent[]) : [];
  const eventsText = eventList.length === 0
    ? '(this time range has no events)'
    : eventList.map(e => {
        const start = new Date(e.startAt).toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' });
        const end = e.endAt
          ? ` → ${new Date(e.endAt).toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' })}`
          : '';
        return `- ${e.title}: ${start}${end}`;
      }).join('\n');

  const isEn = lang === 'en-US';

  const systemPrompt = isEn
    ? 'You are a helpful calendar assistant. Answer the user\'s question about their schedule concisely in English. 1-2 sentences maximum.'
    : '你是一个日历助手。用简洁自然的中文回答用户关于日程的问题。最多2句话。';

  const rangeDesc = `${rangeStart ?? '?'} to ${rangeEnd ?? '?'}`;
  const userMessage = isEn
    ? `User question: "${question}"\n\nCalendar events (${rangeDesc}):\n${eventsText}`
    : `用户问题：「${question}」\n\n日程安排（${rangeDesc}）：\n${eventsText}`;

  try {
    const upstream = await fetch('https://api.deepseek.com/chat/completions', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'deepseek-chat',
        messages: [
          { role: 'system', content: systemPrompt },
          { role: 'user', content: userMessage },
        ],
        temperature: 0.3,
        max_tokens: 256,
      }),
    });

    if (!upstream.ok) {
      console.error('[LLM summarize] upstream error', upstream.status);
      return new Response('LLM upstream error', { status: 502 });
    }

    const data = await upstream.json() as {
      choices?: { message?: { content?: string } }[];
    };
    const summary = data.choices?.[0]?.message?.content ?? '';
    return Response.json({ summary });
  } catch (err) {
    console.error('[LLM summarize] error', err);
    return new Response('LLM request failed', { status: 502 });
  }
}
```

- [ ] **Step 2: 手动测试端点**

```bash
curl -s -X POST http://localhost:3001/api/llm/summarize \
  -H "Content-Type: application/json" \
  -d '{
    "question": "明天有什么安排",
    "events": [{"id":"1","title":"组会","startAt":"2026-05-31T07:00:00.000Z","createdAt":"","updatedAt":""}],
    "lang": "zh-CN",
    "rangeStart": "2026-05-31T00:00:00.000Z",
    "rangeEnd": "2026-05-31T23:59:59.000Z"
  }' | jq .
```
预期：返回 `{ "summary": "明天有1个安排：组会，上午15点。" }` 类似内容

- [ ] **Step 3: Commit**

```bash
git add app/api/llm/summarize/route.ts
git commit -m "feat: 新增 LLM 查询摘要 API 路由 /api/llm/summarize"
```

---

## Task 6: 更新 VoiceCommandOverlay

**Files:**
- Modify: `components/voice/VoiceCommandOverlay.tsx`

### 6a: 语言切换状态 + useSpeechRecognition 传参

- [ ] **Step 1: 更新 import 和状态**

在 `VoiceCommandOverlay.tsx` 的 import 区域，将 `parseVoiceCommand` 改为导入新函数：

```ts
import { parseVoiceCommandWithLLM } from '@/lib/voice/parseVoiceCommand';
```

（删除原来的 `import { parseVoiceCommand } from '@/lib/voice/parseVoiceCommand'`）

- [ ] **Step 2: 在组件内添加语言状态**

在 `const [deletingId, setDeletingId] = useState<string | null>(null);` 之后添加：

```ts
const [lang, setLang] = useState<'zh-CN' | 'en-US'>('zh-CN');
```

- [ ] **Step 3: 更新 Result 类型，query 新增 aiSummary**

将：
```ts
| { kind: 'query'; date: Date; events: CalendarEvent[] }
```
改为：
```ts
| { kind: 'query'; date: Date; events: CalendarEvent[]; aiSummary?: string }
```

- [ ] **Step 4: 把 lang 传给 useSpeechRecognition**

将：
```ts
const { supported, listening, interimText, error, start, stop } = useSpeechRecognition({
  onResult: (text) => { if (text) void handleCommand(text); },
});
```
改为：
```ts
const { supported, listening, interimText, error, start, stop } = useSpeechRecognition({
  lang,
  onResult: (text) => { if (text) void handleCommand(text); },
});
```

### 6b: 重写 handleCommand 使用 LLM

- [ ] **Step 5: 替换 handleCommand 中的解析调用**

将整个 `handleCommand` 函数替换为：

```ts
async function handleCommand(text: string) {
  setActionError(null);
  setResult(null);
  if (/^退出$|^关闭$|^取消$|^exit$|^close$|^cancel$/i.test(text.trim())) {
    handleClose();
    return;
  }

  setBusy(true);
  try {
    const parsed = await parseVoiceCommandWithLLM(text, lang);

    if (parsed.intent === 'create' || parsed.intent === 'unknown') {
      if (parsed.ambiguities.length > 0) {
        setResult({ kind: 'create-preview', parsed, original: text });
      } else {
        onCreate(text);
      }
      return;
    }

    const targetDate = parsed.hasDate && parsed.startAt ? new Date(parsed.startAt) : new Date();

    if (parsed.intent === 'query') {
      const fetchStart = parsed.queryRangeStart
        ? new Date(parsed.queryRangeStart)
        : dayRange(targetDate)[0];
      const fetchEnd = parsed.queryRangeEnd
        ? new Date(parsed.queryRangeEnd)
        : dayRange(targetDate)[1];

      const events = await fetchRange([fetchStart, fetchEnd]);
      onQuery(targetDate);

      // AI 摘要（失败不影响主流程）
      let aiSummary: string | undefined;
      try {
        const summaryRes = await fetch('/api/llm/summarize', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            question: text,
            events,
            lang,
            rangeStart: fetchStart.toISOString(),
            rangeEnd: fetchEnd.toISOString(),
          }),
        });
        if (summaryRes.ok) {
          const { summary } = await summaryRes.json() as { summary: string };
          aiSummary = summary;
        }
      } catch {}

      setResult({ kind: 'query', date: targetDate, events, aiSummary });
      speak(aiSummary ?? (
        events.length === 0
          ? (lang === 'en-US' ? 'No events found' : `${formatDateCN(targetDate)}没有安排`)
          : (lang === 'en-US'
              ? `Found ${events.length} event${events.length > 1 ? 's' : ''}`
              : `${formatDateCN(targetDate)}有${events.length}个安排`)
      ));
      return;
    }

    // delete / modify
    const events = await fetchRange(wideRange());
    const { results, tier } = matchEvents(events, {
      date: targetDate,
      hasDate: parsed.hasDate,
      title: parsed.title,
    });

    if (results.length === 0) {
      setResult({ kind: 'notfound', intent: parsed.intent as 'delete' | 'modify' });
      speak(lang === 'en-US' ? 'No matching events found' : '这段时间没有安排');
      return;
    }

    const approximate = tier !== 'exact';

    if (parsed.intent === 'modify') {
      if (tier === 'exact' && results.length === 1) {
        onModify(applyModify(results[0], parsed));
        return;
      }
      setResult({
        kind: 'modify-pick',
        events: results,
        approximate,
        pick: (e) => onModify(applyModify(e, parsed)),
      });
      return;
    }

    setResult({ kind: 'delete', events: results, approximate });
  } catch {
    setActionError(lang === 'en-US' ? 'Processing failed, please try again' : '处理失败，请重试');
    speak(lang === 'en-US' ? 'Processing failed, please try again' : '处理失败，请重试');
  } finally {
    setBusy(false);
  }
}
```

### 6c: UI 更新

- [ ] **Step 6: Header 加语言切换按钮**

将 Header 区域改为：

```tsx
{/* Header */}
<div className="flex items-center justify-between px-5 py-4 border-b border-gray-100">
  <span className="text-sm font-medium text-gray-700">🎙 {lang === 'zh-CN' ? '语音输入' : 'Voice Input'}</span>
  <div className="flex items-center gap-2">
    <button
      onClick={() => setLang(l => l === 'zh-CN' ? 'en-US' : 'zh-CN')}
      className="text-xs px-2 py-0.5 rounded-full border border-gray-200 text-gray-500 hover:bg-gray-100 transition"
    >
      {lang === 'zh-CN' ? '中' : 'EN'}
    </button>
    <button
      onClick={handleClose}
      className="w-6 h-6 flex items-center justify-center rounded-full hover:bg-gray-100 text-gray-400 text-lg leading-none"
    >
      ×
    </button>
  </div>
</div>
```

- [ ] **Step 7: query 结果区域加 AI 摘要**

将 `result.kind === 'query'` 的渲染块改为：

```tsx
{result.kind === 'query' && (
  <>
    {result.aiSummary && (
      <div className="rounded-xl bg-blue-50 border border-blue-100 px-4 py-3">
        <p className="text-xs text-blue-400 font-medium mb-1">AI 摘要</p>
        <p className="text-sm text-blue-800 leading-relaxed">{result.aiSummary}</p>
      </div>
    )}
    <p className="text-xs text-gray-400 font-medium uppercase tracking-wide">
      {lang === 'zh-CN'
        ? `${formatDateCN(result.date)} 的安排（${result.events.length}）`
        : `Events on ${result.date.toLocaleDateString('en-US')} (${result.events.length})`}
    </p>
    {result.events.length === 0 ? (
      <p className="text-sm text-gray-500">{lang === 'zh-CN' ? '这天没有安排。' : 'No events found.'}</p>
    ) : (
      <div className="space-y-2 max-h-64 overflow-y-auto">
        {result.events.map((e) => (
          <div key={e.id} className="rounded-xl bg-gray-50 border border-gray-100 px-4 py-3">
            {eventRow(e)}
          </div>
        ))}
      </div>
    )}
  </>
)}
```

- [ ] **Step 8: 更新文字输入 placeholder（支持英文提示）**

将 textarea 的 `placeholder` 改为：

```tsx
placeholder={lang === 'zh-CN'
  ? '用自然语言描述\n例：明天下午3点开组会 / 这周有什么安排'
  : 'Describe in natural language\ne.g. meeting tomorrow at 3pm / what\'s on this week'}
```

- [ ] **Step 9: 更新语音模式提示文字**

将：
```tsx
<p className="text-xs text-gray-400 text-center leading-relaxed">
  试试：明天下午3点开组会 · 删除明天的组会 · 明天有什么安排
</p>
```
改为：
```tsx
<p className="text-xs text-gray-400 text-center leading-relaxed">
  {lang === 'zh-CN'
    ? '试试：明天下午3点开组会 · 删除明天的组会 · 这周有什么安排'
    : 'Try: meeting tomorrow at 3pm · delete tomorrow\'s meeting · what\'s on this week'}
</p>
```

- [ ] **Step 10: 类型检查**

```bash
npx tsc --noEmit
```
预期：无报错

- [ ] **Step 11: 运行全量测试**

```bash
pnpm test
```
预期：全量 PASS

- [ ] **Step 12: 手动验收（dev server 需运行）**

测试以下场景：
1. 语言切换按钮能在「中」和「EN」之间切换，标题文字跟着变
2. 说「明天有什么安排」→ 浮层显示 AI 摘要蓝色区域 + 事件列表
3. 说「what meetings do I have this week」（英文模式）→ 英文摘要显示
4. 断开网络或清空 DEEPSEEK_API_KEY → 仍然能正常创建/删除事件（正则兜底）

- [ ] **Step 13: Commit**

```bash
git add components/voice/VoiceCommandOverlay.tsx
git commit -m "feat: 语音浮层接入 LLM NLU，支持中英切换和 AI 查询摘要"
```

---

## 验收标准

- [ ] 说「下礼拜三下午开个会」能正确创建事件（正则做不到，LLM 可以）
- [ ] 说「这周有什么重要的事」能返回跨多天的摘要，不只是当天
- [ ] 说「add a meeting tomorrow at 3pm」（英文模式）能创建事件，摘要用英文
- [ ] 拔掉网络 / 清空 key → 语音指令仍然可用（正则兜底），不崩溃
- [ ] `pnpm test` 全量通过
- [ ] `npx tsc --noEmit` 无报错
